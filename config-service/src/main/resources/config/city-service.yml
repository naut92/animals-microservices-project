server:
  port: 8082

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8062/eureka/
    region: default

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"


